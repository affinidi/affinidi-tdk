name: vault_example
description: Vault Example
version: 1.6.0
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  affinidi_tdk_vault: ^1.9.0
  affinidi_tdk_vault_data_manager: ^1.8.0
  affinidi_tdk_vault_edge_drift_provider: ^1.0.0-dev.5
  affinidi_tdk_vault_edge_provider: ^1.0.0-dev.5
  base_codecs: ^1.0.1
  ssi: ^2.2.0

dev_dependencies:
  dart_flutter_team_lints: ^3.2.1
  test: ^1.24.0
